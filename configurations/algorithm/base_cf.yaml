defaults:
  - base_subgraph_algo

model:
  edge_index_path: ${dataset.edge_index_path}
  num_layers: ???
  dropout: ???
  conv: ???
  aggr: ???
  normalize: ???

top_k: ${dataset.num_illicits}

test_every_n_epoch: 1
train_with_1_1: true
use_edge_index: true