defaults:
  - base_cf

model:
  num_layers: 3
  dropout: 0
  conv: LightGCN
  aggr: mean
  normalize: false

top_k: ${dataset.num_illicits}

test_every_n_epoch: 1
train_with_1_1: true
use_edge_index: true