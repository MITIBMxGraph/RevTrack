defaults:
  - base_subgraph_algo

model:
  num_layers: 1
  hidden_dim: 128
  dropout: 0.1
  activation: ELU

top_k: ${dataset.num_illicits}

test_every_n_epoch: 1
train_with_1_1: true
use_edge_index: False